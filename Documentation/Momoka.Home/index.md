# Momoka.Home

The home digital-twin model library: 3D space structures, entity & property
systems, spatial queries (line of sight, hitscan, collision, pathfinding) and
serialization. Pure model library — no external runtime services.

- **API reference**: see the left navigation (generated from XML doc comments).
- **Design**: see `Documentation/DESIGN_HOME.md` in the repository.
