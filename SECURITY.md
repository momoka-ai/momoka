# 安全政策（Security Policy）

## 支持的版本

本项目目前处于**早期开发阶段，尚未发布任何正式版本**。以下为版本支持策略：

| 版本 | 支持情况 |
|------|----------|
| `main` 分支 | ✅ 接受安全修复（合并至 `main`） |
| 正式发布版本 | ⏳ 首个正式版本发布后适用（详见 [CHANGELOG.md](CHANGELOG.md)） |

## 报告漏洞

**请勿在公开的 Issue 或 PR 中披露安全漏洞。** 请通过以下方式私下报告：

1. 打开一个**私密的**安全通告（GitHub Security Advisory）：`仓库 → Security → Report a vulnerability`。
2. 或通过维护者在仓库主页公布的邮箱联系（如暂未公布，请使用安全通告）。

请尽量在报告中包含：

- 漏洞类型与严重性描述；
- 受影响模块与文件路径；
- 复现步骤（或最小 PoC）；
- 受影响的版本 / 分支。

## 处理流程

1. **确认**：维护者会在 7 天内确认收到报告并评估。
2. **修复**：在私有分支修复并编写回归测试。
3. **发布**：合入 `main` 并（如适用）在下一个版本中发布安全修复。
4. **披露**：修复发布后，在 [CHANGELOG.md](CHANGELOG.md) 与安全通告中公开说明。

## 安全相关设计

Momoka 涉及家庭设备控制，物理安全是核心关切（详见 [ROADMAP.md](ROADMAP.md) 中 Momoka.Home 的 `Security` 规划）：

- **L0–L4 分级安全过滤**：危险操作（燃气、门锁、高压设备）将被拦截。
- **本地优先**：核心推理在本地进行（Ollama），减少数据外传。
- **最小权限**：设备控制仅通过统一抽象层执行，受约束校验。

在以上机制落地前，请勿在生产环境连接真实的高风险设备。
